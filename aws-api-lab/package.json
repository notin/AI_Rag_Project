{
  "name": "aws-api-lab",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@9.12.0",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "test": "pnpm -r --if-present test",
    "lint:openapi": "redocly lint openapi/assets@v1.yaml"
  },
  "devDependencies": {
    "@redocly/cli": "^1.34.0"
  }
}
